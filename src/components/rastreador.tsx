"use client";
// El tracking de la página pública: avisa de la apertura al cargar y manda el
// tiempo de cada sección al salir de ella y al cerrar la pestaña.
//
// DUEÑO: carril C. La fase 5 le pone dentro el IntersectionObserver y el
// sendBeacon contra /api/lectura.
export function Rastreador({ token }: { token: string }) {
  void token;
  return null;
}
